#include <bits/stdc++.h>
using namespace std;


int main(void){
    ios::sync_with_stdio(0);
    cin.tie(0);
    int t,s;
    cin >> t >> s;

    if(s == 0 && (t >= 12 && t <= 16)){
        cout << "320";
    }

    else{
        cout << "280";
    }

}